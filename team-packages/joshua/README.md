# Joshua — frontend/responsive-ui

## Your Branch
`frontend/responsive-ui`

## Your Task
Make the main webapp UI fully responsive for mobile and tablet screens.

## Files To Edit In Your VS Code
Navigate to these exact files in your PythonMP folder:

```
PythonMP/
 └── webapp/
      ├── static/
      │    ├── script.js        ← edit this
      │    └── style.css        ← edit this
      └── templates/
           └── index.html       ← edit this
```

## Specific Things To Fix
1. Sidebar should collapse on mobile (screen width below 768px)
2. Plotly charts should resize properly on small screens
3. Font sizes should scale down on mobile
4. The dashboard grid should stack vertically on small screens

## How To Push
```bash
git checkout frontend/responsive-ui
git add .
git commit -m "feat: responsive layout for mobile and tablet"
git push origin frontend/responsive-ui
```

## Rules
- DO NOT touch any files outside webapp/static/ and webapp/templates/
- DO NOT touch app.py, database/, or simulation/
